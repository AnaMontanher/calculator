const equalButton = document.querySelector(".btn-equal");
const numberButton = document.querySelector(".btn-number");
const operatorButton = document.querySelector(".btn-operator");
const result = document.querySelector(".display");

const resultadoConta = 

numberButton.addEventListener("click", function (number){
    number.preventDefault();
    result.textContent = numberButton;

} 
)



equalButton.addEventListener("click", function (final) {
    final.preventDefault();


 result.textContent = resultadoConta;

});
